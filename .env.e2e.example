# E2E harness config. Copy to .env.e2e and fill in for the real-testnet tier.
# The mock tier (npm run test:e2e:mock) needs NONE of these.

# One-knob environment (harness side). "testnet" or "mock".
E2E_NETWORK=testnet

# Funded Sepolia test key (throwaway) — signs real deposits/claims. Its USDC is
# minted fresh each run via the token's public mint(); top up its Sepolia ETH
# from a faucet (global-setup fails fast below the floor).
E2E_EVM_PRIVATE_KEY=0x...

# Funded Miden testnet signer (the send flows). SEED + the account id it funds.
E2E_MIDEN_SEED=0x...
E2E_MIDEN_ACCOUNT_ID=mtst1...   # bech32 or 0x hex

# REQUIRED for the SEND specs: hex of the account file EXPORTED from MidenFi.
# MidenFi accounts are private, and the Miden SDK cannot reconstruct a private
# account from its seed — only the exported account file carries the account's
# keys + private-note context needed to see/claim bridged notes and sign sends.
# (Receive specs don't need this.) Export it from MidenFi, hex-encode the bytes.
E2E_MIDEN_ACCOUNT_FILE=

# Optional overrides
# E2E_SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
# MIDEN_CLIENT_BIN=miden-client          # path to the version-matched CLI
# E2E_AGENTIC=true                        # leave server+browser open on failure
# NEXT_PUBLIC_E2E_SIGNER_MODE=sign        # or "reject" to force a 4001

# NOTE: the Playwright e2e config forwards E2E_EVM_PRIVATE_KEY / E2E_MIDEN_SEED /
# E2E_MIDEN_ACCOUNT_ID into the SUT build as NEXT_PUBLIC_* (the injected signers
# run in the browser). These are throwaway testnet accounts and only present in
# an E2E build — the flag is off (and vars absent) in production.
